/*private static JFrame win;
    public static void theWin(String winner) {
        win = new JFrame();
        win.setBounds(Consts.SIX_HUNDRED + Consts.HUNDRED * 2, Consts.HUNDRED * 2, Consts.HUNDRED, Consts.HUNDRED);
        win.setDefaultCloseOperation(JFrame.DISPOSE_ON_CLOSE);
        win.add(new PainterForWinner(winner));

        JButton ok = new JButton("OKey, if you say so...");
        win.add(ok, BorderLayout.SOUTH);

        ok.addActionListener(new Ok(ok));
    }

    public static void theWinClose() {
        win.dispose();
    }

    static class PainterForWinner extends JComponent {

        private static String winner;

        public PainterForWinner(String winner) {
            this.winner = winner;
        }

        protected void paintComponent(Graphics g) {
            Graphics2D g2 = (Graphics2D) g;

            Font font = new Font("DejaVu Sans", Font.BOLD, Consts.FONT_SIZE);
            g2.setFont(font);
            g2.setPaint(Color.blue);
            int hun = Consts.HUNDRED / 2 / 2;

            g2.drawString("Winner is...", hun, hun);

            font = new Font("DejaVu Sans", Font.BOLD, Consts.FONT_SIZE * 2);
            g2.setFont(font);
            g2.setPaint(Color.pink);
            g2.drawString(winner, hun * 2, hun);
        }
    }*/